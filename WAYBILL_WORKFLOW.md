# جریان‌کاری حالت وضعیت بارنامه‌ها

## خلاصه تغییرات

این آپدیت یک جریان‌کاری جدید برای مدیریت وضعیت بارنامه‌ها اضافه می‌کند که شامل مراحل تایید توسط متصدی‌های مبدا و مقصد است.

## وضعیت‌های جدید

1. **ثبت شده** (Registered) - وضعیت اولیه
2. **بارگیری شده** (Loading) - ⭐ جدید
3. **ارسال شده** (Sent) - منتظر پایان سفر
4. **پایان پیمایش** (Trip Completed) - ⭐ جدید
5. **تحویل شده** (Delivered) - وضعیت نهایی

## جریان‌کاری دقیق

### مرحله ۱: راننده شروع سفر می‌کند
- **ورودی:** راننده دکمه "شروع سفر" را در `driver_waybills.php` می‌زند
- **فایل:** `driver_waybills.php` → `waybill_geofence_check.php` → `api/trip_action.php`
- **نتیجه:** وضعیت تغییر می‌یابد: `ثبت شده` → `بارگیری شده`
- **پیام:** "سفر با موفقیت شروع شد. منتظر تایید متصدی مبدا برای ارسال بارنامه باشید."

### مرحله ۲: متصدی مبدا بارنامه را تایید می‌کند
- **ورودی:** متصدی مبدا وارد صفحه `operator_loading_public.php` می‌شود
- **لیست:** تمام بارنامه‌های با وضعیت `بارگیری شده` که متصدی مبدای آنها است
- **عملیات:** دکمه "تایید و ارسال" را می‌زند
- **فایل:** `operator_loading_public.php` → `api/operator_approve_loading.php`
- **نتیجه:** وضعیت تغییر می‌یابد: `بارگیری شده` → `ارسال شده`
- **پیام:** "بارنامه با موفقیت برای ارسال تایید شد."

### مرحله ۳: راننده سفر را تمام می‌کند
- **ورودی:** راننده دکمه "پایان سفر" را در `driver_waybills.php` می‌زند
- **فایل:** `driver_waybills.php` → `waybill_geofence_check.php` → `api/trip_action.php`
- **نتیجه:** وضعیت تغییر می‌یابد: `ارسال شده` → `پایان پیمایش`
- **پیام:** "سفر با موفقیت به پایان رسید. منتظر تایید متصدی مقصد برای تحویل بارنامه باشید."

### مرحله ۴: متصدی مقصد بارنامه را تحویل می‌گیرد
- **ورودی:** متصدی مقصد وارد صفحه `operator_delivery_public.php` می‌شود
- **لیست:** تمام بارنامه‌های با وضعیت `پایان پیمایش` که متصدی مقصدی آنها است
- **عملیات:** دکمه "تایید تحویل" را می‌زند
- **فایل:** `operator_delivery_public.php` → `api/operator_approve_delivery.php`
- **نتیجه:** وضعیت تغییر می‌یابد: `پایان پیمایش` → `تحویل شده`
- **پیام:** "بارنامه با موفقیت به عنوان تحویل‌شده ثبت شد."

## فایل‌های تغیر یافته

### API
- **`api/trip_action.php`**
  - تغییر شروع سفر: `ثبت شده` → `بارگیری شده`
  - تغییر پایان سفر: `ارسال شده` → `پایان پیمایش`
  - پیام‌های بهتر برای هر مرحله

### صفحه‌های عمومی (جدید)
- **`operator_loading_public.php`** - صفحه متصدی مبدا برای تایید بارگذاری
- **`operator_delivery_public.php`** - صفحه متصدی مقصد برای تایید تحویل

### API Endpoints (جدید)
- **`api/operator_approve_loading.php`** - تایید بارگذاری توسط متصدی مبدا
- **`api/operator_approve_delivery.php`** - تایید تحویل توسط متصدی مقصد

### صفحات دیگر
- **`driver_waybills.php`**
  - اضافه کردن وضعیت `بارگیری شده` به لیست نمایش‌شونده
  - نمایش پیام‌های منتظر تایید
  - اپدیت `statusClassMap`

- **`waybill_geofence_check.php`**
  - بهبود پیام‌های خطا برای وضعیت‌های نامعتبر

## دسترسی‌های الزام‌ی

### متصدی مبدا
```
متصدی باید این شرایط را داشته باشد:
- origin_operator_user_id در جدول fuel_waybills برابر با id متصدی
- سطح دسترسی: operator
- حساب فعال
```

### متصدی مقصد
```
متصدی باید این شرایط را داشته باشد:
- destination_operator_user_id در جدول fuel_waybills برابر با id متصدی
- سطح دسترسی: operator
- حساب فعال
```

## مثال استفاده

### برای راننده
```
1. وارد شوید با توکن یا کد ملی/رمز عبور: driver_waybills.php?token=xxx
2. بارنامه خود را پیدا کنید
3. دکمه "شروع سفر" را بزنید
4. بعد از بارگذاری (تایید متصدی مبدا)، دکمه "پایان سفر" را بزنید
```

### برای متصدی مبدا
```
1. وارد شوید: operator_loading_public.php?token=xxx
2. لیست بارنامه‌های درحال‌بارگذاری را مشاهده کنید
3. برای هر بارنامه، دکمه "تایید و ارسال" را بزنید
```

### برای متصدی مقصد
```
1. وارد شوید: operator_delivery_public.php?token=xxx
2. لیست بارنامه‌های پایان‌شده را مشاهده کنید
3. برای هر بارنامه، دکمه "تایید تحویل" را بزنید
```

## نکات مهم

1. **توکن‌ها:** همه اپی‌ها و صفحات از سیستم توکن محافظ‌شده استفاده می‌کنند
2. **اعتبار:** شامل بررسی وضعیت فعال حساب کاربری و سطح دسترسی
3. **امن:** کل جریان‌کاری بدون نیاز به سشن‌ها کار می‌کند
4. **خطاهای واضح:** هر خطا با پیام‌های شفاف و در فارسی برگردانده می‌شود

## تست کردن

### تست شروع سفر
```bash
curl -X POST http://localhost/seal/api/trip_action.php \
  -H "Content-Type: application/json" \
  -d '{"token":"YOUR_DRIVER_TOKEN", "waybill_id":1}'
```

### تست تایید بارگذاری
```bash
curl -X POST http://localhost/seal/api/operator_approve_loading.php \
  -H "Content-Type: application/json" \
  -d '{"token":"YOUR_ORIGIN_OPERATOR_TOKEN", "waybill_id":1}'
```

### تست تایید تحویل
```bash
curl -X POST http://localhost/seal/api/operator_approve_delivery.php \
  -H "Content-Type: application/json" \
  -d '{"token":"YOUR_DEST_OPERATOR_TOKEN", "waybill_id":1}'
```
